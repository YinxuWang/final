import {pad} from './pad';

/**
 * Generate auto increment userId according to type
 * @param type 'phone' Or 'mail'
 * @param userId user.userId
 * @returns {*} increased userId
 */
export function fn(type, userId) {
  if (type === 'mail') {

  } else {


  }
  return userId
}

export const TYPE = {
  PHONE: 'phone',
  MAIL: 'mail'
};
